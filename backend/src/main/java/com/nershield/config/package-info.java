/**
 * Cross-cutting Spring configuration that is not owned by a single feature package.
 *
 * <p>Feature-specific configuration lives with its feature - for example
 * {@link com.nershield.ai.AIProperties} and {@link com.nershield.security.JwtProperties}.
 */
package com.nershield.config;
