/**
 * Incident records: what happened, where, when and at what severity.
 *
 * <p>Not implemented yet. This package will hold the {@code Incident} entity with its
 * PostGIS location column, the repository, the service owning status transitions, and the
 * controller. Incidents are the hub the report, alert and response domains attach to.
 */
package com.nershield.incident;
