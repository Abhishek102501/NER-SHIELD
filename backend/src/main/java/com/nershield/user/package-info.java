/**
 * User accounts, roles and organisational units.
 *
 * <p>Not implemented yet. This package will hold the {@code User} entity, its repository,
 * the service that owns account lifecycle rules, and the controller exposing user
 * management to the frontend. It is the source of the principals that
 * {@link com.nershield.security.JwtTokenProvider} will authenticate.
 */
package com.nershield.user;
